# ─────────────────────────────────────────────────────────────────────────────
# Dataflow backend — variables de entorno
# Host "db" es el nombre del contenedor PostgreSQL en Docker Compose.
# Si corrés el backend fuera de Docker, cambiá "db" por "localhost".
# ─────────────────────────────────────────────────────────────────────────────

DATABASE_URL=postgresql://dataflow:dataflow123@db:5432/dataflow
SESSION_SECRET=supersecretocambiame
UPLOAD_DIR=./uploads
PORT=3001
FRONTEND_URL=http://localhost:5173
